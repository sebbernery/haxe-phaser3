package phaser.blendmodes;

/**
 * Copy blend mode. For Canvas only.
 * Only the new shape is shown.
 *
 * @name Phaser.BlendModes.COPY
 */
@:native("Phaser.BlendModes.COPY")
extern class COPY {
    public function new();
}
