package phaser.blendmodes;

/**
 * Add blend mode. For Canvas and WebGL.
 * Where both shapes overlap the color is determined by adding color values.
 *
 * @name Phaser.BlendModes.ADD
 */
@:native("Phaser.BlendModes.ADD")
extern class ADD {
    public function new();
}
