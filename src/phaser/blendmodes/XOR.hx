package phaser.blendmodes;

/**
 * xor blend mode. For Canvas only.
 * Shapes are made transparent where both overlap and drawn normal everywhere else.
 *
 * @name Phaser.BlendModes.XOR
 */
@:native("Phaser.BlendModes.XOR")
extern class XOR {
    public function new();
}
