package phaser;

/**
 * @namespace Phaser.Tweens
 */
@:native("Phaser.Tweens")
class Tweens {
    /**
     * TweenData state.
     *
     * @name Phaser.Tweens.CREATED
     * @type {integer}
     * @since 3.0.0
     */
    static public var CREATED:Int;
    /**
     * TweenData state.
     *
     * @name Phaser.Tweens.INIT
     * @type {integer}
     * @since 3.0.0
     */
    static public var INIT:Int;
    /**
     * TweenData state.
     *
     * @name Phaser.Tweens.DELAY
     * @type {integer}
     * @since 3.0.0
     */
    static public var DELAY:Int;
    /**
     * TweenData state.
     *
     * @name Phaser.Tweens.OFFSET_DELAY
     * @type {integer}
     * @since 3.0.0
     */
    static public var OFFSET_DELAY:Int;
    /**
     * TweenData state.
     *
     * @name Phaser.Tweens.PENDING_RENDER
     * @type {integer}
     * @since 3.0.0
     */
    static public var PENDING_RENDER:Int;
    /**
     * TweenData state.
     *
     * @name Phaser.Tweens.PLAYING_FORWARD
     * @type {integer}
     * @since 3.0.0
     */
    static public var PLAYING_FORWARD:Int;
    /**
     * TweenData state.
     *
     * @name Phaser.Tweens.PLAYING_BACKWARD
     * @type {integer}
     * @since 3.0.0
     */
    static public var PLAYING_BACKWARD:Int;
    /**
     * TweenData state.
     *
     * @name Phaser.Tweens.HOLD_DELAY
     * @type {integer}
     * @since 3.0.0
     */
    static public var HOLD_DELAY:Int;
    /**
     * TweenData state.
     *
     * @name Phaser.Tweens.REPEAT_DELAY
     * @type {integer}
     * @since 3.0.0
     */
    static public var REPEAT_DELAY:Int;
    /**
     * TweenData state.
     *
     * @name Phaser.Tweens.COMPLETE
     * @type {integer}
     * @since 3.0.0
     */
    static public var COMPLETE:Int;
    /**
     * Tween state.
     *
     * @name Phaser.Tweens.PENDING_ADD
     * @type {integer}
     * @since 3.0.0
     */
    static public var PENDING_ADD:Int;
    /**
     * Tween state.
     *
     * @name Phaser.Tweens.PAUSED
     * @type {integer}
     * @since 3.0.0
     */
    static public var PAUSED:Int;
    /**
     * Tween state.
     *
     * @name Phaser.Tweens.LOOP_DELAY
     * @type {integer}
     * @since 3.0.0
     */
    static public var LOOP_DELAY:Int;
    /**
     * Tween state.
     *
     * @name Phaser.Tweens.ACTIVE
     * @type {integer}
     * @since 3.0.0
     */
    static public var ACTIVE:Int;
    /**
     * Tween state.
     *
     * @name Phaser.Tweens.COMPLETE_DELAY
     * @type {integer}
     * @since 3.0.0
     */
    static public var COMPLETE_DELAY:Int;
    /**
     * Tween state.
     *
     * @name Phaser.Tweens.PENDING_REMOVE
     * @type {integer}
     * @since 3.0.0
     */
    static public var PENDING_REMOVE:Int;
    /**
     * Tween state.
     *
     * @name Phaser.Tweens.REMOVED
     * @type {integer}
     * @since 3.0.0
     */
    static public var REMOVED:Int;
}
