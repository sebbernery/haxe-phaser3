package phaser.physics.impact.type;

/**
 * Collides with both types A and B.
 *
 * @name Phaser.Physics.Impact.TYPE.BOTH
 */
@:native("Phaser.Physics.Impact.TYPE.BOTH")
extern class BOTH {
    public function new();
}
