package phaser.scale.scalemodes;

/**
 * The Canvas is resized to fit all available _parent_ space, regardless of aspect ratio.
 *
 * @name Phaser.Scale.ScaleModes.RESIZE
 * @since 3.16.0
 */
@:native("Phaser.Scale.ScaleModes.RESIZE")
extern class RESIZE {
    public function new();
}
