// Define a Main class, with a main method where your game will be launched.

class Main {
    static public function main() {
            window.addEventListener("load", function () {
            Game.start_game();
        });
    }
}